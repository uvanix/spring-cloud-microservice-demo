spring:
  application:
    name: cloud-service-bbb

server:
  port: ${PORT:9002}

eureka:
  client:
    serviceUrl:
      defaultZone: http://user:password@localhost:8761/eureka/

feign:
  hystrix:
    enabled: true

logging:
  level:
    com:
      uvanix: debug
